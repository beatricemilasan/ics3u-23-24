package lesson3;

public class ExampleThree {
    public static void main(String[] args) {
        int mark1 = 84, mark2 = 93, mark3 = 97;
        int numTests = 3;

        // three types of errors:

        // logic errors: program runs, doesn't crash but you don't get the expected output
        // runtime errors: runs then crashes when it is running ex: dividing by 0
        // syntax errors: won't run (compile), red squiggle, error in java code, forgetting a "" or maybe ;

        // arithematic except = problem with math
        
    }
}
